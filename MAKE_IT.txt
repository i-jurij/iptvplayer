скрипт сборки аппимейдж кидает в корень файл usr/share/appimage/какие то там данные
- куда его правильно положить? - сделано, не проверено

проверить подписывание пакетов при локальной сборке - не проверено

переписать скрипт сборки зависимостей, чтобы можно было собрать в любой системе линукс -
сделано, не тестировалось

добавит секрет на гитхаб
починить сборку на гитхаб - зависимости и флаги сборки виксвиджетс (поправить скрипты)
==========================================================================

сделать рамку фокуса ри виде сеткой, потому что хауэр сбивает с толку - 
непонятно почему программа для одного канала, а рамка над другим. 
Сделать его такого цвета как хауэр, а хауэр более приглушенным.

===========================================================================
добавить матчинг избранного в общий прогресс добавления/обновления источника.

================================================================================

проверить логику матчинга избранного. Когда он нужен, когда нет.
когда просто грузить маппинг.
сделать отмену старого, если вызван новый из рефрешсоурсес.

## Уточненный пошаговый план улучшения матчинга избранного

---

### Шаг 1. Добавление полей и методов в EPGManager (подготовка)

**Файлы:** `EPGManager.h`, `EPGManager.cpp`

**Действия:**
1. Добавить в класс `EPGManager` приватные поля:
   - `std::atomic<bool> m_cancelFavoritesMatching{false};`
   - `std::future<void> m_favoritesMatchFuture;`
   - `std::string m_lastFavoritesEpgHash;` – кэш хэша в памяти
   - `std::mutex m_favoritesHashMutex;` – защита доступа к хэшу

2. Добавить методы (объявление в `.h`, реализация в `.cpp`):
   - `void LoadFavoritesEpgHashFromDB();` – загружает из БД по ключу `"favorites_epg_hash"`
   - `void SaveFavoritesEpgHashToDB(const std::string& hash);` – сохраняет в БД
   - `bool IsFavoritesEpgHashChanged() const;` – сравнивает `m_epgChannelsHash` с загруженным

3. В конструкторе `EPGManager` (после открытия БД) вызвать `LoadFavoritesEpgHashFromDB()`.

4. В `EPGManager::OpenDatabase()` при создании новой БД (или при очистке кэша) сбросить хэш в БД (удалить запись или записать пустую строку).

---

### Шаг 2. Реализация `MatchFavoritesAsync` с отменой и проверкой хэша

**Файл:** `EPGManager.cpp`

**Действия:**
1. Изменить сигнатуру метода на `void MatchFavoritesAsync(bool force = false);` (объявление в `.h` тоже обновить).

2. Реализовать логику:
   - В начале проверить `m_favoritesMatchInProgress.exchange(true)`. Если уже `true`:
     - Установить `m_cancelFavoritesMatching = true`.
     - Дождаться завершения `m_favoritesMatchFuture` с таймаутом 2 секунды (если future валиден).
     - Сбросить `m_cancelFavoritesMatching = false`.
     - Сбросить `m_favoritesMatchInProgress = false`.
   - Затем проверить условие для пропуска:
     - Если `!m_loaded` – выйти (EPG не загружен).
     - Если `!force && !IsFavoritesEpgHashChanged()` – выйти с логом "Favorites EPG hash unchanged, skipping".
   - Запустить асинхронную задачу (`std::async`), в которой:
     - Получить список избранных каналов через `Application::getFavoritesManager().list()`.
     - Если список пуст – выйти, сохранив хэш? (можно сохранить, чтобы в следующий раз не проверять).
     - Для каждого канала проверить `m_cancelFavoritesMatching` – если `true`, прервать.
     - Выполнить матчинг (использовать `FindBestMatch` для каждого канала и сохранять маппинги через `AddAutoMapping` или напрямую в БД).
     - После успешного завершения сохранить новый хэш: `m_lastFavoritesEpgHash = m_epgChannelsHash; SaveFavoritesEpgHashToDB(m_lastFavoritesEpgHash);`
   - В конце (в деструкторе или после) сбросить `m_favoritesMatchInProgress = false`.

3. Добавить логирование на каждом этапе (пропуск, отмена, начало, успех).

---

### Шаг 3. Оптимизация fallback по URL (без полного перебора всех плейлистов)

**Файл:** `EPGManager.cpp` (внутри асинхронной задачи)

**Действия:**
1. Собрать множество названий плейлистов, которые присутствуют в избранных каналах (чтобы не перебирать все плейлисты).
2. Для каждого такого плейлиста получить его каналы и построить временный индекс `url -> tvgId` или `url -> epgId` (если есть маппинг).
3. В цикле по избранным каналам, если не найден маппинг по tvgId или имени, использовать этот индекс.
4. Внутри всех циклов периодически проверять `m_cancelFavoritesMatching` и прерывать работу.

---

### Шаг 4. Корректировка мест вызова `MatchFavoritesAsync`

**Файлы:** `EPGManager.cpp`, `MainFrame.cpp`, `EpgSourceManagerPanel.cpp`

**Действия:**
1. **EPGManager::UpdateAllSources** – в колбэке завершения после матчинга плейлиста добавить вызов `MatchFavoritesAsync(false);`.
2. **MainFrame::loadPlaylistChannels** – убрать прямой вызов `MatchFavoritesAsync()` (там он был лишним, т.к. матчинг избранного всё равно запустится после обновления EPG или по запросу пользователя). Если требуется немедленный матчинг, вызвать с `false` – но он сам проверит хэш.
3. **EpgSourceManagerPanel::RefreshSourceInternal** – оставить вызов `m_epgMgr->MatchFavoritesAsync(false);` (уже есть).
4. **EPGManager::ReMatchCurrentPlaylist** – для случая `m_currentPlaylistId == FAVORITES_PLAYLIST_ID` изменить вызов на `MatchFavoritesAsync(true);` (принудительный рематчинг при нажатии «Apply»).
5. В остальных местах (если есть) убедиться, что `MatchFavoritesAsync` вызывается с правильным параметром.

---

### Шаг 5. Обновление информационного сообщения о матчинге (About)

**Файл:** `EpgSourceManagerPanel.cpp` (метод `OnAboutMatch`)

**Действия:**
1. Добавить в текст сообщения абзац о ручном рематчинге:
   - Для текущего плейлиста: переключиться на вкладку Channels и нажать «Apply (Match Now)».
   - Для избранного: переключиться на вкладку Favorites и нажать «Apply (Match Now)».
   - Автоматический рематчинг происходит после обновления EPG-источников, но только если изменился список каналов EPG (проверка хэша).

---

### Шаг 6. Добавление логирования и тестирование

**Действия:**
1. Добавить логи в ключевых точках:
   - `LOG_DEBUG` при пропуске матчинга из-за хэша.
   - `LOG_DEBUG` при отмене старого матчинга.
   - `LOG_DEBUG` при запуске/завершении асинхронной задачи.
2. Проверить сценарии:
   - Добавление нового EPG-источника → матчинг избранного запускается (если хэш изменился).
   - Нажатие «Apply» на вкладке Favorites → матчинг запускается принудительно (force=true).
   - Повторное нажатие «Apply» во время выполнения → старый матчинг отменяется, запускается новый.
   - Если EPG не изменился (хэш тот же) → матчинг не запускается автоматически.
   - Избранное пусто → матчинг пропускается.

---

### Шаг 7. Дополнительно (опционально): кнопка для сброса хэша избранного

Если потребуется, можно добавить кнопку «Reset favorites cache» в разделе EPG, которая удалит сохранённый хэш из БД, чтобы при следующем обновлении матчинг избранного обязательно запустился. Но это не обязательно, т.к. пользователь может принудительно запустить матчинг через Apply.

---

### Итог

- **Отменяемость**: реализована через флаг отмены и ожидание завершения.
- **Эффективность**: проверка хэша предотвращает ненужные перезапуски.
- **Удобство**: единая кнопка Apply для текущего контекста (плейлист или избранное) с параметром force=true.
- **Информированность**: обновлено сообщение About, пользователь знает о поведении.

Все изменения локализованы в `EPGManager` и местах вызова, не затрагивают другие модули.

## Полный набор изменений для реализации улучшенного матчинга избранного

Ниже представлены все необходимые изменения по файлам. Вставьте их последовательно.

---

### 1. `EPGManager.h`

**В конец `private`-секции добавить:**

```cpp
    // Управление матчингом избранного
    std::atomic<bool> m_cancelFavoritesMatching{false};
    std::future<void> m_favoritesMatchFuture;
    std::string m_lastFavoritesEpgHash;
    std::mutex m_favoritesHashMutex;

    void LoadFavoritesEpgHashFromDB();
    void SaveFavoritesEpgHashToDB(const std::string& hash);
    bool IsFavoritesEpgHashChanged() const;

public:
    // Изменить сигнатуру метода (добавить параметр force)
    void MatchFavoritesAsync(bool force = false);
```

---

### 2. `EPGManager.cpp`

#### 2.1. В конструкторе после открытия БД загрузить хэш

В конце метода `EPGManager::OpenDatabase()` после `m_loaded` и `UpdateEpgNameCache()` добавить:

```cpp
    LoadFavoritesEpgHashFromDB();
```

#### 2.2. Реализация методов работы с хэшем

Добавить в конец файла (или в соответствующее место) следующие реализации:

```cpp
void EPGManager::LoadFavoritesEpgHashFromDB() {
    std::lock_guard<std::recursive_mutex> dbLock(m_dbMutex);
    if (!m_db || !m_db->IsOpen()) return;
    std::lock_guard<std::mutex> hashLock(m_favoritesHashMutex);
    m_lastFavoritesEpgHash = m_db->LoadGlobalMetadata("favorites_epg_hash");
    LOG_DEBUG("EPGManager: Loaded favorites EPG hash: '%s'", m_lastFavoritesEpgHash.c_str());
}

void EPGManager::SaveFavoritesEpgHashToDB(const std::string& hash) {
    std::lock_guard<std::recursive_mutex> dbLock(m_dbMutex);
    if (!m_db || !m_db->IsOpen()) return;
    if (m_db->SaveGlobalMetadata("favorites_epg_hash", hash)) {
        LOG_DEBUG("EPGManager: Saved favorites EPG hash: '%s'", hash.c_str());
    } else {
        LOG_ERROR("EPGManager: Failed to save favorites EPG hash");
    }
}

bool EPGManager::IsFavoritesEpgHashChanged() const {
    std::lock_guard<std::mutex> hashLock(m_favoritesHashMutex);
    return m_epgChannelsHash != m_lastFavoritesEpgHash;
}
```

#### 2.3. Полная переработка `MatchFavoritesAsync`

**Заменить существующую реализацию на следующую:**

```cpp
void EPGManager::MatchFavoritesAsync(bool force) {
    // 1) Проверка/отмена предыдущего матчинга
    bool expected = false;
    if (!m_favoritesMatchInProgress.compare_exchange_strong(expected, true)) {
        LOG_DEBUG("MatchFavoritesAsync: already in progress, cancelling old task");
        m_cancelFavoritesMatching = true;

        if (m_favoritesMatchFuture.valid()) {
            auto status = m_favoritesMatchFuture.wait_for(std::chrono::seconds(2));
            if (status == std::future_status::timeout) {
                LOG_WARN("MatchFavoritesAsync: old task did not finish within 2s, forcing new start");
            }
        }

        m_cancelFavoritesMatching = false;
        m_favoritesMatchInProgress = false;

        expected = false;
        if (!m_favoritesMatchInProgress.compare_exchange_strong(expected, true)) {
            LOG_WARN("MatchFavoritesAsync: race condition, aborting");
            return;
        }
    }

    // 2) Проверка необходимости матчинга
    if (!m_loaded) {
        LOG_DEBUG("MatchFavoritesAsync: EPG not loaded, skipping");
        m_favoritesMatchInProgress = false;
        return;
    }

    if (!force && !IsFavoritesEpgHashChanged()) {
        LOG_DEBUG("MatchFavoritesAsync: favorites EPG hash unchanged, skipping");
        m_favoritesMatchInProgress = false;
        return;
    }

    // 3) Запуск асинхронной задачи
    m_favoritesMatchFuture = std::async(std::launch::async, [this, force]() {
        LOG_DEBUG("MatchFavoritesAsync: starting background task (force=%d)", force);

        Application* app = static_cast<Application*>(wxTheApp);
        if (!app) {
            LOG_ERROR("MatchFavoritesAsync: Application is null");
            m_favoritesMatchInProgress = false;
            return;
        }

        auto favChannels = app->getFavoritesManager().list();
        if (favChannels.empty()) {
            LOG_DEBUG("MatchFavoritesAsync: no favorites, saving current hash");
            std::lock_guard<std::mutex> hashLock(m_favoritesHashMutex);
            m_lastFavoritesEpgHash = m_epgChannelsHash;
            SaveFavoritesEpgHashToDB(m_lastFavoritesEpgHash);
            m_favoritesMatchInProgress = false;
            return;
        }

        // Собираем названия плейлистов, которые есть в избранном
        std::unordered_set<std::string> playlistNames;
        for (const auto& ch : favChannels) {
            if (!ch.getPlaylistName().empty())
                playlistNames.insert(ch.getPlaylistName());
        }

        // Строим индексы:
        // 1) playlistName -> playlistId
        // 2) playlistName -> (url -> tvgId)
        std::unordered_map<std::string, std::string> nameToId;
        std::unordered_map<std::string, std::unordered_map<std::string, std::string>> urlToTvgId;
        if (m_playlistManager) {
            for (const auto& pl : m_playlistManager->getPlaylists()) {
                const std::string& name = pl->getTitle();
                if (playlistNames.find(name) == playlistNames.end())
                    continue;
                nameToId[name] = pl->getUniqueId();
                auto& urlMap = urlToTvgId[name];
                for (const auto& ch : pl->getChannels()) {
                    if (!ch.getTvgId().empty() && !ch.getUrl().empty()) {
                        urlMap[ch.getUrl()] = ch.getTvgId();
                    }
                }
            }
        }

        // Основной цикл по избранным каналам
        int matched = 0;
        std::string favoritesPlaylistId = FAVORITES_PLAYLIST_ID;
        for (const auto& favCh : favChannels) {
            if (m_cancelFavoritesMatching) break;

            std::string key = favCh.getTvgId();
            if (key.empty())
                key = "name:" + NormalizeName(favCh.getName());

            // Проверяем, есть ли уже ручной маппинг – пропускаем
            std::string existingEpgId;
            bool isManual = false;
            if (GetMappingEntry(favoritesPlaylistId, key, existingEpgId, isManual) && isManual)
                continue;

            // Ищем наилучшее совпадение
            MatchResult match = FindBestMatch(favCh);
            if (!match.channelId.empty()) {
                AddAutoMapping(favoritesPlaylistId, key, match.channelId);
                matched++;
                continue;
            }

            // Fallback: поиск по URL (оптимизированный через индекс)
            const std::string& playlistName = favCh.getPlaylistName();
            if (!playlistName.empty() && !favCh.getUrl().empty()) {
                auto itName = urlToTvgId.find(playlistName);
                if (itName != urlToTvgId.end()) {
                    auto itUrl = itName->second.find(favCh.getUrl());
                    if (itUrl != itName->second.end()) {
                        std::string tvgId = itUrl->second;
                        auto itId = nameToId.find(playlistName);
                        if (itId != nameToId.end()) {
                            std::string sourcePlaylistId = itId->second;
                            std::string epgId;
                            bool isManualSrc = false;
                            if (GetMappingEntry(sourcePlaylistId, tvgId, epgId, isManualSrc) && !epgId.empty()) {
                                AddAutoMapping(favoritesPlaylistId, key, epgId);
                                matched++;
                                LOG_DEBUG("MatchFavoritesAsync: fallback URL match for '%s' -> '%s' (via %s)",
                                          favCh.getName().c_str(), epgId.c_str(), tvgId.c_str());
                            }
                        }
                    }
                }
            }

            if (m_cancelFavoritesMatching) break;
        }

        // Сохраняем хэш, если матчинг завершился успешно (не был отменён)
        if (!m_cancelFavoritesMatching) {
            std::lock_guard<std::mutex> hashLock(m_favoritesHashMutex);
            m_lastFavoritesEpgHash = m_epgChannelsHash;
            SaveFavoritesEpgHashToDB(m_lastFavoritesEpgHash);
            LOG_DEBUG("MatchFavoritesAsync: completed, matched %d favorites", matched);
        } else {
            LOG_DEBUG("MatchFavoritesAsync: cancelled, not saving hash");
        }

        m_favoritesMatchInProgress = false;

        wxCommandEvent evt(EVT_FAVORITES_MATCH_DONE);
        evt.SetInt(matched);
        evt.SetExtraLong(static_cast<int>(favChannels.size()));
        wxQueueEvent(wxTheApp->GetTopWindow(), evt.Clone());
    });
}
```

#### 2.4. Изменения в `EPGManager::DeleteCache()`

В конце метода после создания новой БД добавить сброс хэша:

```cpp
    // Сброс хэша избранного
    {
        std::lock_guard<std::mutex> hashLock(m_favoritesHashMutex);
        m_lastFavoritesEpgHash.clear();
        SaveFavoritesEpgHashToDB("");
    }
```

#### 2.5. Изменение `EPGManager::UpdateAllSources()`

Внутри колбэка после `MatchChannelsAsync` добавить вызов `MatchFavoritesAsync(false)`:

```cpp
            if (anySuccess->load() && !m_currentPlaylistId.empty() && m_playlistManager) {
                Playlist* pl = m_playlistManager->findByUniqueId(m_currentPlaylistId);
                if (pl && !pl->getChannels().empty()) {
                    MatchChannelsAsync(pl->getChannels(), m_currentPlaylistId, nullptr);
                    // Добавить вызов матчинга избранного
                    MatchFavoritesAsync(false);
                }
            }
```

#### 2.6. Изменение `EPGManager::ReMatchCurrentPlaylist()`

Заменить блок для избранного на:

```cpp
    if (m_currentPlaylistId == FAVORITES_PLAYLIST_ID) {
        LOG_DEBUG("ReMatchCurrentPlaylist: rematching favorites");
        MatchFavoritesAsync(true);
        return;
    }
```

---

### 3. `EpgSourceManagerPanel.cpp`

#### 3.1. Обновление `OnAboutMatch()`

Вставить новый абзац перед заключительной частью про advanced tuning:

```cpp
void EpgSourceManagerPanel::OnAboutMatch(wxCommandEvent &) {
  wxString msg =
      _("EPG Matching Algorithm\n\n"
        "1. Manual mappings (highest priority)\n"
        "2. TVG-ID match (if present)\n"
        "3. Exact name match (after normalization)\n"
        "4. Substring match (if one name contains the other)\n"
        "5. Token-sort (percentage of common words)\n"
        "6. Jaro-Winkler refinement for borderline cases\n\n"
        "Adjustable parameters (visible below):\n"
        "  • Fuzzy threshold (50–100) – influences Jaro-Winkler bonus\n"
        "  • Substring min length – minimum length for substring match\n"
        "  • Substring ratio – percentage threshold (not used directly, kept "
        "for compatibility)\n"
        "  • Min match score – required score (0–100) to accept a match\n\n"
        "Score thresholds:\n"
        "  • 85+ : High confidence\n"
        "  • 70+ : Medium confidence\n"
        "  • 60+ : Low confidence\n\n"
        "Manual rematch:\n"
        "  • To force rematching of the current playlist, switch to the "
        "Channels tab and click 'Apply (Match Now)'.\n"
        "  • To force rematching of favorites, switch to the Favorites tab "
        "and click 'Apply (Match Now)'.\n"
        "  • Automatic rematching is triggered after EPG source updates "
        "and only runs if the EPG channel list has changed (hash check).\n\n"
        "For advanced tuning, you can manually edit the configuration files\n"
        "‘matching_rules.json’ located in the config directory.\n"
        "The default settings are already optimised – it is strongly "
        "recommended to use\n"
        "manual mappings or aliases (in ‘channel_aliases.json’) for specific "
        "corrections instead of modifying these files.");
  wxMessageBox(msg, _("About EPG Matching"), wxOK | wxICON_INFORMATION, this);
}
```

#### 3.2. `RefreshSourceInternal` – оставить вызов `MatchFavoritesAsync(false)` (это уже есть, можно не менять)

---

### 4. `MainFrame_PlaylistLogic.cpp`

#### 4.1. Удалить дублирующий вызов `MatchFavoritesAsync()`

В функции `loadPlaylistChannels` удалить строку:

```cpp
epg->MatchFavoritesAsync();   // <-- удалить
```

Оставить только `epg->MatchChannelsAsync(...)`.

---

### 5. Дополнительно: убедиться, что в `MainFrame.cpp` определён `EVT_FAVORITES_MATCH_DONE`

В файле `MainFrame.cpp` уже присутствует:

```cpp
wxDEFINE_EVENT(EVT_FAVORITES_MATCH_DONE, wxCommandEvent);
```

Этот код не требует изменений.

---

## Итог

Все изменения перечислены. После внесения их в проект, матчинг избранного будет:
- Отменяемым при повторном вызове.
- Пропускать выполнение, если EPG не изменился (хэш-проверка).
- Использовать оптимизированный индекс для fallback по URL.
- Принудительно запускаться через кнопку «Apply (Match Now)» на вкладке Favorites.
- Автоматически запускаться после обновления EPG-источников (с проверкой хэша).
